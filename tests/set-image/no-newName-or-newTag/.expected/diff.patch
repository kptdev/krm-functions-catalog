diff --git a/Kptfile b/Kptfile
index 00be8d7..634ff85 100644
--- a/Kptfile
+++ b/Kptfile
@@ -7,3 +7,25 @@ pipeline:
     - image: ghcr.io/kptdev/krm-functions-catalog/set-image:latest
       configMap:
         name: foo
+status:
+  conditions:
+    - type: Rendered
+      status: "False"
+      reason: RenderFailed
+      message: |-
+        pkg.render: pkg .:
+        	pipeline.run: already handled error
+  renderStatus:
+    mutationSteps:
+      - image: ghcr.io/kptdev/krm-functions-catalog/set-image:latest
+        stderr: 'failed to evaluate function: error: function failure'
+        exitCode: 1
+        results:
+          - message: 'invalid FunctionConfig: must specify one of `newName`, `newTag`, or `digest`'
+            severity: error
+          - message: 'summary: updated a total of 0 image(s)'
+            severity: info
+        errorResults:
+          - message: 'invalid FunctionConfig: must specify one of `newName`, `newTag`, or `digest`'
+            severity: error
+    errorSummary: 'ghcr.io/kptdev/krm-functions-catalog/set-image:latest: exit code 1'
