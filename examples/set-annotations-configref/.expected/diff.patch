diff --git a/Kptfile b/Kptfile
index 60d8fb9..27d0a34 100644
--- a/Kptfile
+++ b/Kptfile
@@ -3,7 +3,9 @@ kind: Kptfile
 metadata:
   name: example
   annotations:
+    color: orange
     config.kubernetes.io/local-config: "true"
+    fruit: apple
 pipeline:
   mutators:
     - image: ghcr.io/kptdev/krm-functions-catalog/set-annotations:latest
@@ -11,3 +13,34 @@ pipeline:
         apiVersion: fn.kpt.dev/v1alpha1
         kind: SetAnnotations
         name: my-func-config
+status:
+  conditions:
+    - type: Rendered
+      status: "True"
+      reason: RenderSuccess
+  renderStatus:
+    mutationSteps:
+      - image: ghcr.io/kptdev/krm-functions-catalog/set-annotations:latest
+        exitCode: 0
+        results:
+          - message: 'set annotations: {"color":"orange","fruit":"apple"}'
+            field:
+              path: metadata.annotations
+            file:
+              path: Kptfile
+          - message: 'set annotations: {"color":"orange","fruit":"apple"}'
+            field:
+              path: metadata.annotations
+            file:
+              path: fn-config.yaml
+          - message: 'set annotations: {"color":"orange","fruit":"apple"}'
+            field:
+              path: metadata.annotations
+            file:
+              path: resources.yaml
+          - message: 'set annotations: {"color":"orange","fruit":"apple"}'
+            field:
+              path: metadata.annotations
+            file:
+              path: resources.yaml
+              index: 1
diff --git a/fn-config.yaml b/fn-config.yaml
index 2164b7e..f1432c3 100644
--- a/fn-config.yaml
+++ b/fn-config.yaml
@@ -4,6 +4,8 @@ metadata:
   name: my-func-config
   annotations:
     config.kubernetes.io/local-config: "true"
+    color: orange
+    fruit: apple
 annotations:
   color: orange
   fruit: apple
diff --git a/resources.yaml b/resources.yaml
index 7a5cb40..4e2c418 100644
--- a/resources.yaml
+++ b/resources.yaml
@@ -2,6 +2,9 @@ apiVersion: v1
 kind: ConfigMap
 metadata:
   name: the-map
+  annotations:
+    color: orange
+    fruit: apple
 data:
   some-key: some-value
 ---
@@ -9,3 +12,6 @@ apiVersion: v1
 kind: Namespace
 metadata:
   name: ns
+  annotations:
+    color: orange
+    fruit: apple
